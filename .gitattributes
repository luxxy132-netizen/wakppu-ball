# 맥에서 실행되는 스크립트는 반드시 LF 여야 한다.
# CRLF 로 바뀌면 /bin/bash^M 을 찾다가 bad interpreter 오류가 난다.
*.command text eol=lf
*.sh      text eol=lf

# 윈도우 배치는 CRLF
*.bat     text eol=crlf
*.ps1     text eol=crlf

*.mp3     binary
*.ico     binary
*.icns    binary
*.png     binary
